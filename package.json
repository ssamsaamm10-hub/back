{
  "name": "readeasy-arabic-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API + AI layer for ReadEasy Arabic (Nebras) — Node/Express + SQLite + Gemini",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "db:reset": "rm -f data/dev.db && node src/db/migrate.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "zod": "^3.23.8"
  }
}
